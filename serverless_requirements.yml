service: TFG_Booking-application-requirements

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    layer:
      name: ${self:provider.stage}-requirements-layer
      description: Python requirements lambda layer
      compatibleRuntimes:
        - python3.9
    dockerizePip: true
    dockerImage: public.ecr.aws/sam/build-python3.9:latest
    fileName: requirements.txt
    slim: true

provider:
  name: aws
  runtime: python3.9
  region: eu-west-3
  stage: ${opt:stage, 'develop'}
  profile: ${opt:aws-profile, 'booking_cli'}

package:
  patterns:
    - "!**/**"
